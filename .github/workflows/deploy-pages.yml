name: Deploy Cooking Confidential staging

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages-staging
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Check out staging workflow repository
        uses: actions/checkout@v4

      - name: Check out Cooking Confidential development branch
        uses: actions/checkout@v4
        with:
          repository: sunilbane19/CookingConfidential
          ref: egress-optimization
          path: site

      - name: Remove production custom domain
        run: rm -f site/CNAME

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5
        with:
          enablement: true

      - name: Upload staging site
        uses: actions/upload-pages-artifact@v3
        with:
          path: site

      - name: Deploy staging site
        id: deployment
        uses: actions/deploy-pages@v4

# Food52 retry + true duplicate detection 2026-09-20
# Cache refresh 2026-09-19
# Auth redirect refresh 2026-09-19
# App cache refresh 2026-09-19
  # Sign-in redirect refresh 2026-09-19
# Upload review + queue refresh 2026-09-19

# Upload handler syntax fix + DOCX review cache refresh 2026-09-19

# Recipe description + default card image consistency 2026-09-19

# URL import queue fix 2026-09-19

# Selective import upload controls 2026-09-19
# Import Inbox source link labels + URL extraction refresh 2026-09-19 + Main authenticated Import Inbox loader 2026-09-19
# auth-error-path-fix-1789833088626
# restore-known-good-app-1789883160903

# Import review syntax fix 2026-09-20

# Import review navigation + clarity refresh 2026-09-20

# Cache bump for import review 2026-09-20

# Retry failed imports + extractor timeout 2026-09-20

# Import review image field 2026-09-20

# Import formatting preservation + HTML entity decode 2026-09-20

# Import review image search matches Edit 2026-09-20

# Import review image picker load + extraction error diagnostics 2026-09-20

# Scanned PDF OCR structure refresh 2026-09-20

# Refresh import review for corrected scanned PDF OCR 2026-09-20

# Fix scanned PDF method boundary + stale OCR refresh 2026-09-20

# Force scanned PDF OCR refresh on every Review 2026-09-20

# Fix import inbox loading stale review module 2026-09-20

# Restore rich text tools in Review 2026-09-20

# Fix Review field duplication and restore Notes 2026-09-20
